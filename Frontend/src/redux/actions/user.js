import { REGISTER_USER_REQUEST, REGISTER_USER_SUCCESS, REGISTER_USER_FAILURE } from "../constants/user"
import axios from "axios"
axios.defaults.withCredentials = true;

const registeruserAction = (formData) => async () => {
    const Response = ""
    try {
        
    } catch (error) {
        
    }
  
}

